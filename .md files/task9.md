# Фаза 9 — Полировка и деплой

**Статус:** 🔄 В работе (код готов, деплой не выполнен)
**Связанные файлы:** cursor.md (команды деплоя), SPEC.md (полный список функциональности)
**Зависит от:** все предыдущие фазы (task1-task8) должны быть завершены

---

## Цель фазы

## Обработка всех edge cases, финальное тестирование всех сценариев из SPEC.md,
настройка SSL и деплой на продакшн сервер.

## Skills для этой фазы


| Skill                      | Когда активировать                                                      |
| -------------------------- | ----------------------------------------------------------------------- |
| **mastering-typescript**   | При финальной проверке TypeScript кода                                  |
| **owasp-security**         | При финальном security review                                           |
| **code-review-excellence** | При финальном code review перед деплоем                                 |
| **systematic-debugging**   | При отладке edge cases                                                  |
| **code-reviewer**          | Для итоговой приоритизации дефектов/рисков по изменениям перед релизом  |
| **testing-reviewer**       | Для аудита полноты тестов и выявления слабых/хрупких проверок           |
| **api-contract-checker**   | Для финальной сверки API контрактов frontend/backend                    |
| **spec-driven-workflow**   | Для контроля закрытия фазы и фиксации финального состояния документации |


### Когда skill указывать явно

- Явно указывать **code-reviewer**/**testing-reviewer** перед финальным merge/deploy.
- Явно указывать **api-contract-checker**, если были правки API в предыдущих фазах.
- Явно указывать **owasp-security**, если есть auth/upload/download/network-sensitive изменения.

---

## Задачи

- Обработка ошибок на фронтенде (toast уведомления)
- Обработка ошибок на бэкенде (единый формат)
- Fallback если OSRM недоступен
- Fallback если Photon недоступен
- Проверить все сценарии из SPEC.md вручную
- Тесты бэкенда (Jest) — авторизация, API
- Тесты фронтенда (Vitest) — компоненты
- Настроить nginx конфиг для mapvideo.gyhyry.ru
- SSL через Certbot
- Финальный деплой docker compose на сервер
- Проверка работоспособности на продакшне

---

## Обработка ошибок (фронтенд)

Все ошибки показывать через toast уведомления в правом нижнем углу:

```typescript
// Типы уведомлений
type: 'error' | 'warning' | 'success'

// Примеры сообщений:
// "Не удалось найти адрес. Проверьте подключение к интернету."
// "Маршрут по дороге недоступен. Построена прямая линия."
// "Видео успешно создано и скачано."
// "Ошибка рендера. Попробуйте ещё раз."
```

---

## Fallback сценарии

**OSRM недоступен** (проверяется в task6.md):
Если запрос к `/api/route` вернул ошибку — показать toast
"Маршрут по дороге временно недоступен. Построена прямая линия."
и построить прямую линию между точками.

**Photon недоступен** (проверяется в task5.md):
Если геосаджест не отвечает — скрыть список подсказок,
пользователь может вручную ввести название и нажать "Продолжить".
В этом случае точка ставится в центр текущего вида карты.

---

## Чеклист финальной проверки

**Полный сценарный чеклист с чекбоксами, полями окружения/даты и блоком «после деплоя»:** [docs/manual-qa-checklist.md](docs/manual-qa-checklist.md).

Сохраняйте прогресс в этом файле (в репозитории или копии в тикет).

---

## Деплой на сервер

### 1. Nginx конфиг (хостовой — docker-nginx нет)

**КРИТИЧНО: не редактировать существующие конфиги maps.gyhyry.ru!** Создать только новый файл.
**КРИТИЧНО: не поднимать nginx в Docker.** Используется только системный nginx хоста.

```bash
sudo nano /etc/nginx/sites-available/mapvideo.gyhyry.ru.conf
# Вставить конфиг из task1.md
sudo ln -s /etc/nginx/sites-available/mapvideo.gyhyry.ru.conf /etc/nginx/sites-enabled/
sudo nginx -t            # обязательно — если падает, откатить ТОЛЬКО свой файл
sudo systemctl reload nginx
```

### 2. SSL

```bash
sudo certbot --nginx -d mapvideo.gyhyry.ru
```

### 3. Код уже на сервере

Репозиторий живёт в `~/mapvideo` с самого начала разработки (работа ведётся по SSH).
На этапе деплоя нужно только:

```bash
cd ~/mapvideo
git pull
cp .env.example .env      # если .env ещё не создан
# Заполнить .env — POSTGRES_PASSWORD, ADMIN_PASSWORD, COOKIE_SECURE=true, OSM_PBF_FILE
nano .env
```

### 4. Импортировать данные OSM (первый раз)

```bash
# По умолчанию Россия (~3-4 GB, 20-40 минут)
./scripts/import-osm.sh
# Если нужен весь мир: поменять OSM_PBF_FILE=planet-latest.osm.pbf в .env, несколько часов
```

### 5. Запустить

```bash
docker compose up -d
docker compose ps   # все должны быть healthy
```

### 6. Финальная проверка на продакшне

Наружу видны только 80/443. Внутренние сервисы — через `docker compose exec`.

```bash
# Снаружи (то что видит пользователь)
curl https://mapvideo.gyhyry.ru/api/health
curl https://mapvideo.gyhyry.ru/tiles/catalog

# Внутренние (127.0.0.1 — только с сервера)
curl http://127.0.0.1:3001/api/health
curl http://127.0.0.1:3002/catalog
curl "http://127.0.0.1:5000/route/v1/driving/37.618,55.751;30.315,59.939"

# Геокодер — публичный Photon (Photon в compose нет)
curl "https://photon.komoot.io/api?q=Москва&limit=1"
```

---

## Что делать после деплоя

1. Зайти под admin, создать пользователей для редакции
2. Проверить что ассеты из `/assets/` корректно читаются фронтендом и рендером
3. При необходимости настроить стиль карты через Maputnik

---

## Заметка для следующей сессии

*(заполняется завершения задачи или перед завершением сессии)*